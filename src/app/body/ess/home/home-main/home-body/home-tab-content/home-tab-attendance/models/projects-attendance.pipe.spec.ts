import { ProjectsAttendancePipe } from './projects-attendance.pipe';

describe('ProjectsAttendancePipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectsAttendancePipe();
    expect(pipe).toBeTruthy();
  });
});
