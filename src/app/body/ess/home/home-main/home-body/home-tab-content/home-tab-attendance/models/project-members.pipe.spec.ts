import { ProjectMembersPipe } from './project-members.pipe';

describe('ProjectMembersPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectMembersPipe();
    expect(pipe).toBeTruthy();
  });
});
