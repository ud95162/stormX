import { ProjectDataObject } from './project-data-object';

describe('ProjectDataObject', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new ProjectDataObject()).toBeTruthy();
  });
});
