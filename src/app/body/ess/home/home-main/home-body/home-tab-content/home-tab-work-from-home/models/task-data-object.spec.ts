import { TaskDataObject } from './task-data-object';

describe('TaskDataObject', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new TaskDataObject()).toBeTruthy();
  });
});
