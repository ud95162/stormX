import { ResponseDataObject } from './response-data-object';

describe('ResponseData', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new ResponseDataObject()).toBeTruthy();
  });
});
