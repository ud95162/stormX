import { SharedFilterObject } from './shared-filter-object';

describe('SharedFilterObject', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new SharedFilterObject()).toBeTruthy();
  });
});
