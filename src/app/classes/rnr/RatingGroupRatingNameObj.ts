import {RatingGroupRatingName} from './RatingGroupRatingName';
import {RatingGroup} from './RatingGroup';
import {RatingName} from './RatingName';

export interface RatingGroupRatingNameObj {
  ratingGroupRatingId: RatingGroupRatingName;
  ratingGroup: RatingGroup;
  ratingName: RatingName;
}
