import { UserGroupFilterPipePipe } from './user-group-filter-pipe.pipe';

describe('UserGroupFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UserGroupFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
