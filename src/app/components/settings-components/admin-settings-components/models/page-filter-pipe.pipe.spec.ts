import { PageFilterPipePipe } from './page-filter-pipe.pipe';

describe('PageFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PageFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
