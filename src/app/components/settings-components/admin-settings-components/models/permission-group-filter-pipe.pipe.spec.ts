import { PermissionGroupFilterPipePipe } from './permission-group-filter-pipe.pipe';

describe('PermissionGroupFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PermissionGroupFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
