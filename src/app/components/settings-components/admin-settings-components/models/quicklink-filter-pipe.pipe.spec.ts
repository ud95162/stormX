import { QuicklinkFilterPipePipe } from './quicklink-filter-pipe.pipe';

describe('QuicklinkFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new QuicklinkFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
