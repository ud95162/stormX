import { DesignationFilterPipePipe } from './designation-filter-pipe.pipe';

describe('DesignationFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DesignationFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
